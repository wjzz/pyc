// swap using tmp variable (using a function and pointers)

int swap(long* x, long* y) {
  long tmp = *x;
  *x = *y;
  *y = tmp;
}

int main() {
  long a = 15;
  long b = 10;

  print(a);
  print(b);

  long unused = swap(&a, &b);
 
  print(a);
  print(b);

  return 0;
}
